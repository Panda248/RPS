		var choices = ["Rock", "Paper", "Scissors"];
		var plrInputStr;
		var comInput;
		var plrInputInt;
function main() {
	plrInputStr = document.getElementById("userInput");
	console.log(plrInputStr);
	plrInputInt = parseInt(plrInputStr);
	console.log(plrInputInt);
	comInput = Math.floor(Math.random() * 3) + 1;
}
function calcResult (x, y)	{
	document.getElementsByClassName('Results');
	switch (x,y) {
		case x === 1 && y === 3:


			break;
		default:

	}
}
